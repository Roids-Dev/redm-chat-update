# redm-chat-update

This is just an update to the `chat` and `poke_rpchat` resources
The only thing it does is make it so the only things that work in chat are commands such as /ooc or any other commands you have on your server
